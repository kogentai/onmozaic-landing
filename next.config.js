/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  // Optional: if your site is served from a subpath (e.g. username.github.io/repo-name), set basePath: '/repo-name'
  // basePath: '',
  // trailingSlash: true,  // uncomment if GitHub Pages has issues with client-side routes
}

export default nextConfig
